#define TRUE 1
#define FALSE 0

#include "C_files/video.c"
#include "C_files/keyboard.c"
#include "C_files/builtin.c"

void main() {
	screen_setup();
	loadWelcome();
	printint(1);
	println("");
	printint(2);
println("");
	printint(0);
println("");
	printint(-1);
println("");
	while(TRUE){
		typePrompt();
		readCommand();
//		executeCommand();
	}
}

